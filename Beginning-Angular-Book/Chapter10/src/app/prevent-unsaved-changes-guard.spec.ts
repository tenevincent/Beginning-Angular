import { PreventUnsavedChangesGuard } from './prevent-unsaved-changes-guard';

describe('PreventUnsavedChangesGuard', () => {
  it('should create an instance', () => {
    expect(new PreventUnsavedChangesGuard()).toBeTruthy();
  });
});
